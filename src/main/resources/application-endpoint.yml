endpoint:
  permit-all:
    get:
    post:
    put:
    patch:
    delete:
  roles:
    - matcher:
        get:
        post:
        put:
        patch:
        delete:
      role: user, mentor
    - matcher:
        get:
        post: /api/v1/events
        put:
        patch: /api/v1/events/*, /api/v1/events/*/mentee/*
        delete:
      role: mentor
