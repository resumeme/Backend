endpoint:
  permit-all:
    get: /api/v1/user
    post:
    put:
    patch:
    delete:
  roles:
    - matcher:
        get: /api/v1/user
        post:
        put:
        patch:
        delete:
      role: user, mentor
    - matcher:
        get: /api/v1/user
        post:
        put:
        patch:
        delete:
      role: mentor
